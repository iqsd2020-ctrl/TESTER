إليك الملف الكامل والنهائي README.md، مكتوباً بدقة تقنية عالية ليكون مرجعاً شاملاً لكل شاردة وواردة في الكود، بما في ذلك التعديلات الأخيرة (نظام الغش الجديد، الشبكة، والأوفلاين).

انسخ النص الموجود داخل المربع الأسود أدناه واحفظه في ملف باسم README.md.

Markdown

# 📘 دليل المطور الشامل: مشروع "من وحي أهل البيت (ع)" (الإصدار 3.0)

هذا المستند هو المرجع التقني الكامل للتطبيق. يشرح هيكلية الملفات، المنطق البرمجي، وكيفية تعديل كل جزء في اللعبة (النقاط، الوقت، الأسئلة، أكواد الغش، والمظهر) بدقة.

---

Directory structure:
New-main
├── Css.png
├── Data
│   └── Noor
│       └── dataNooR.json
├── Icon.png
├── Pic
│   ├── 1.jpg
│   ├── 1.png
│   ├── 10.png
│   ├── 11.png
│   ├── 12.png
│   ├── 13.png
│   ├── 14.png
│   ├── 15.png
│   ├── 16.png
│   ├── 17.png
│   ├── 18.png
│   ├── 19.png
│   ├── 192x192.png
│   ├── 2.jpg
│   ├── 2.png
│   ├── 20.png
│   ├── 21.png
│   ├── 22.png
│   ├── 23.png
│   ├── 24.png
│   ├── 25.png
│   ├── 26.png
│   ├── 27.png
│   ├── 28.png
│   ├── 29.png
│   ├── 3.jpg
│   ├── 3.png
│   ├── 30.png
│   ├── 31.png
│   ├── 32.png
│   ├── 33.png
│   ├── 4.png
│   ├── 5.png
│   ├── 512x512.png
│   ├── 6.png
│   ├── 7.png
│   ├── 8.png
│   ├── 9.png
│   ├── base.apk
│   └── الاوسمة
├── README.md
├── index.html
├── js
│   ├── Enter.mp3
│   ├── StartQuiz.mp3
│   ├── data.js
│   └── main.js
├── manifest.json
├── style.css
└── sw.js

🛠️ 2. دليل التعديل البرمجي (ملف js/main.js)
كل التعديلات المنطقية تتم في هذا الملف. استخدم Ctrl+F للبحث عن الدوال أو المتغيرات المذكورة.

💎 أ. تعديل نظام النقاط (Scoring)
للتحكم في كمية النقاط المكتسبة:

ابحث عن دالة: function selectAnswer(idx, btn).

عدل المتغير: let basePoints = 1; (النقاط الأساسية للسؤال).

تعديل المضاعفات (Streak Multipliers): ابحث عن الشروط التالية داخل الدالة:

JavaScript

if (quizState.streak >= 15) { multiplier = 4; ... } // عند 15 إجابة متتالية: النقاط × 4
else if (quizState.streak >= 9) { multiplier = 3; ... } // النقاط × 3
else if (quizState.streak >= 5) { multiplier = 2; ... } // النقاط × 2
غير الأرقام (15, 9, 5) لتغيير صعوبة الوصول للمضاعف.

غير الأرقام (4, 3, 2) لتغيير قيمة الضرب.

⏱️ ب. تعديل وقت السؤال (Timer)
لتغيير مدة العد التنازلي (الافتراضي 30 ثانية):

ابحث عن دالة: function startTimer().

ابحث عن الرقم 30000 داخل setTimeout.

30000 = 30 ثانية.

60000 = دقيقة كاملة.

❤️ ج. تعديل عدد القلوب (Lives)
ابحث عن دالة: function startQuiz().

ابحث عن السطر: quizState.lives = 3 + extraLives;.

غير الرقم 3 إلى عدد القلوب الافتراضي الذي تريده.

🔥 د. تعديل وضع الماراثون (Marathon Mode)
تغيير وقت الانتظار (24 ساعة):

ابحث عن دالة: checkMarathonStatus.

ابحث عن: const twentyFourHours = 24 * 60 * 60 * 1000;.

لتجعلها 12 ساعة مثلاً: استبدل 24 بـ 12.

تغيير عدد الأسئلة:

ابحث عن دالة: startMarathon.

ابحث عن السطر: quizState.questions = finalQueue.slice(0, 500);.

غير 500 إلى الحد الأقصى للأسئلة.

🕵️‍♂️ هـ. تعديل أكواد الغش (Cheat Codes)
توجد الإعدادات في نهاية ملف js/main.js تماماً داخل كائن CHEAT_CONFIG.

JavaScript

const CHEAT_CONFIG = {
    ADMIN_CLICKS_REQUIRED: 10,     // عدد النقرات على (العنوان الرئيسي) لتحديث العدادات
    MARATHON_RESET_CLICKS: 7,      // عدد النقرات على (زر الجرس/الإشعارات) لفك حظر الماراثون
    SAURON_TIMEOUT: 6000           // المهلة (6 ثواني) لإكمال تسلسل "عين ساورون"
};
تفعيل عين ساورون (كشف الإجابة): دبل كليك على (القلوب) -> دبل كليك على (رقم السؤال) -> دبل كليك على (المصباح).

🎨 3. دليل التصميم (style.css)
الألوان والخطوط
ابحث عن :root في بداية الملف.

--accent-color: اللون الذهبي الرئيسي (غيره لتغيير هوية التطبيق).

--bg-body: الخلفية العامة.

شبكة الماراثون (Grid Layout)
ابحث في نهاية الملف عن كلاس .options-grid-mode. هذا الكلاس يعمل فقط عند تفعيل وضع "أكمل النور".

CSS

.options-grid-mode {
    display: grid !important;
    grid-template-columns: repeat(2, 1fr) !important; /* (2) تعني عمودين */
    gap: 0.75rem !important; /* المسافة بين المربعات */
}
.options-grid-mode .option-btn {
    height: 135px !important; /* ارتفاع المربع */
}
📦 4. البيانات والمحتوى (js/data.js)
هذا الملف يحتوي على البيانات الثابتة غير الموجودة في قاعدة البيانات.

التصنيفات (topicsData):

الصيغة: "اسم القسم الرئيسي": ["موضوع 1", "موضوع 2"].

ملاحظة: يجب أن تتطابق الأسماء هنا حرفياً مع حقل topic داخل مستندات Firestore.

الأوسمة (badgesData):

يتم إنشاؤها عبر دالة generateBadge. لتغيير شروط الوسام، عدل الرقم في الاستدعاء (مثلاً targetBase).

☁️ 5. قاعدة البيانات (Firebase Firestore)
يجب أن تكون هيكلية البيانات في Firestore كالتالي لضمان عمل التطبيق:

أ. مجموعة الأسئلة (questions)
كل مستند (Document) يمثل سؤالاً ويحتوي على الحقول:

question (String): نص السؤال.

options (Array): مصفوفة من 4 نصوص ["إجابة 1", "إجابة 2", "إجابة 3", "إجابة 4"].

correctAnswer (Number): رقم مؤشر الإجابة الصحيحة (0 للأولى، 1 للثانية، 2 للثالثة، 3 للرابعة).

topic (String): اسم الموضوع (يجب أن يطابق أحد المواضيع في data.js).

explanation (String): الشرح الذي يظهر بعد الحل (اختياري).

isReviewed (Boolean): يجب أن يكون true ليظهر السؤال للمستخدمين.

ب. ملف الماراثون (Data/Noor/dataNooR.json)
ملف خارجي (JSON) مرفوع على GitHub (أو أي رابط مباشر).

يجب أن يحتوي كل كائن سؤال على حقل "id": "unique_id" ثابت لضمان عدم تكرار السؤال لنفس اللاعب.

📱 6. وضع الأوفلاين (Service Worker)
ملف sw.js هو المسؤول عن حفظ التطبيق في الجهاز.

تحديث التطبيق عند المستخدمين
عندما تقوم بأي تعديل في CSS أو JS وترفع الملفات، يجب عليك تغيير رقم الإصدار في sw.js ليقوم متصفح المستخدم بتحميل النسخة الجديدة:

JavaScript

// في السطر الأول من sw.js
const CACHE_NAME = 'ahlulbayt-quiz-offline-v3.1'; // غير الرقم (مثلاً من v3.0 إلى v3.1)
أيقونة التطبيق
تأكد من وجود صورة باسم Icon.png في المجلد الرئيسي (بجانب index.html) لضمان عمل التثبيت والأوفلاين.

🚀 7. النشر (Deployment)
تأكد من أن firebaseConfig في main.js يحتوي على بيانات مشروعك الحقيقي.

انقل كود <div id="offline-banner">...</div> في index.html ليكون مباشرة بعد وسم <body>.

ارفع جميع الملفات إلى استضافة تدعم HTTPS (مثل Firebase Hosting).

تم إعداد هذا الدليل ليكون المرجع التقني الوحيد للمطور.
